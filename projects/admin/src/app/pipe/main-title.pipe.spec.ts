import { MainTitlePipe } from './main-title.pipe';

describe('MainTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new MainTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
